- clean: ['~']

- link: 
    ~/.bash: bash/
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.tmux : tmux
    ~/.tmuxgo : tmuxgo
    ~/.irbrc : irbrc

